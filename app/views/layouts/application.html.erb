<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CrowdCloud</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= yield(:google_maps_api) if content_for?(:google_maps_api) %>
  </head>
  <body>

    <div class="navbar navbar-inverse navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#"><b><i class="fa fa-cloud"></i> CrowdCloud</b></a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li class="<%= 'active' if controller.controller_name == "devices" %>">
              <%= link_to "<i class=\"fa fa-laptop\"></i> Devices".html_safe, devices_path %>
            </li>
            <li class="<%= 'active' if controller.controller_name == "sensing_tasks" || controller.controller_name == "responses" %>">
              <%= link_to "<i class=\"fa fa-rss\"></i> Sensing tasks".html_safe, sensing_tasks_path %>
            </li>            
            <li class="<%= 'active' if controller.controller_name == "social_networks" %>">
              <%= link_to "<i class=\"fa fa-group\"></i> Social Networks".html_safe, "#" %>
            </li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-user"></i>
                <%= "#{current_account.first_name} #{current_account.last_name}" %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to "<i class=\"fa fa-sign-out\"></i> Sign out".html_safe, destroy_account_session_path, :method => :delete %>
                </li>
              </ul>
            </li>          
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div>

    </div>

  </body>
</html>
