<ol class="breadcrumb">
  <li><%= link_to "Open calls", open_calls_path %></li>
  <li><%= link_to @open_call.name, open_call_path(@open_call) %></li>
  <li class="active"> Responses</li>
</ol>
<%= render :partial => "open_calls/side_navigation" %>

<div class="col-lg-9">
  <% if @responses.empty? %>
    <div class="alert alert-info" role="alert">
      There are currently no responses for this open call!
    </div>
  <% else %>
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Id</th>
          <th>Device</th>
          <th>Responded at</th>
          <th>Responded data</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @responses.each do |response| %>
          <tr>
            <td><%= response.id %></td>
            <td><%= link_to response.device.uuid, response.device %></td>
            <td><%= response.created_at %></td>
            <td>
              <% response.responded_data_types.each do |type| %>
                <span style="margin-right: 5px;"><%= response_data_type_icon(type).html_safe %></span>
              <% end %>
            </td>
            <td><%= link_to '<i class="fa fa-search"></i> More details'.html_safe, [@open_call, response], :class => "btn btn-primary btn-sm" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
</div>